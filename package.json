{
  "name": "intro-lab-unified",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:search-lab && npm run build:sql-lab",
    "build:search-lab": "cd search-lab-interactive && npm run build",
    "build:sql-lab": "cd sql-to-query-api-lab-interactive && npm run build",
    "start:api": "node server/index.js",
    "postinstall": "cd search-lab-interactive && npm install && cd ../sql-to-query-api-lab-interactive && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mongodb": "^6.3.0",
    "dotenv": "^16.3.0"
  },
  "engines": {
    "node": ">=20.0"
  }
}